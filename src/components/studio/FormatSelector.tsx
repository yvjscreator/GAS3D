import { exportPresets, getExportResolution } from '../../config/exportPresets'
import { useStudioStore } from '../../store/studioStore'
export function FormatSelector() { const { format, setFormat, exportQuality } = useStudioStore(); return <section className="panel"><h2>Formato</h2><div className="format-list">{Object.entries(exportPresets).map(([id, preset]) => { const resolution = getExportResolution(id as typeof format, exportQuality); return <button key={id} className={format === id ? 'format selected' : 'format'} onClick={() => setFormat(id as typeof format)}><span>{preset.label}</span><small>{resolution.width} × {resolution.height}</small></button> })}</div></section> }

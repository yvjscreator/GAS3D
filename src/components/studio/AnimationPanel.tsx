import { useStudioStore } from '../../store/studioStore'
export function AnimationPanel() { const { animation, setAnimation, duration, setDuration, setTargetRotation } = useStudioStore(); return <section className="panel"><h2>Animación</h2><label className="select-row">Movimiento<select value={animation} onChange={(e) => setAnimation(e.target.value as typeof animation)}><option value="spin180">Giro 180°</option><option value="spin360">Giro 360°</option><option value="still">Quieto</option></select></label><label className="range-row">Duración<output>{duration}s</output><input type="range" min="4" max="20" value={duration} onChange={(e) => setDuration(Number(e.target.value))} /></label><div className="quick-views"><button className="secondary small" onClick={() => setTargetRotation(0)}>Frente</button><button className="secondary small" onClick={() => setTargetRotation(Math.PI)}>Espalda</button></div></section> }

import { useStudioStore } from '../../store/studioStore'
export function ExportPanel({ onRecord }: { onRecord: () => void }) { const { recordingStatus, recordingElapsed, duration, recordingMessage } = useStudioStore(); const recording = recordingStatus === 'recording'; return <section className="panel export-panel"><h2>Exportación</h2><p className="muted">30 FPS · video local · sin audio</p>{recording ? <div className="recording">● GRABANDO <strong>{recordingElapsed.toFixed(1)} / {duration}s</strong></div> : <button className="record-button" onClick={onRecord}>● Grabar</button>}{recordingMessage && <p className={recordingStatus === 'error' ? 'error' : 'success'}>{recordingMessage}</p>}</section> }
